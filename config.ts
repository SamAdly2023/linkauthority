/**
 * Feature flags for the public site and dashboard.
 *
 * Pricing is switched off while the network is being seeded: every feature is
 * free until there are enough active sites and registered websites for paid
 * tiers to be worth anything. Flip this back to true to restore the pricing
 * section, the Pricing nav item and the plans page - nothing was deleted.
 */
export const PRICING_ENABLED = false;
